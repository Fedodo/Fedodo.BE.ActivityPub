using ActivityPubServer.Controllers;
using ActivityPubServer.Interfaces;
using Microsoft.Extensions.Logging;
using Moq;
using Serilog;
using Xunit;

namespace ActivityPubServer.Test.Controllers;

public class OutboxControllerShould
{
    [Fact]
    public void CreatePost()
    {
        // Arrange
        //
        // var repository = new Mock<IMongoDbRepository>();
        // OutboxController controller = new OutboxController(logger, repository);

        // Act

        // Assert
    }
}